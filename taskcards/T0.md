# T0 — Protocol shakedown: mean spread by session on the live week

**Status:** complete (harness bootstrap, 2026-08-19)
**Card written by:** user, during the Phase 2 bootstrap
**This is not a research task.** It exists to run the Phase 2 protocol end to
end on data that cannot influence any strategy decision, so that T1 starts on a
path that has already been walked.

---

## Scope

Compute the spread distribution by intraday session for the Phase 1 live week,
through the research loader, the ledger and the research gate.

| | |
|---|---|
| Pairs allowed | `EURUSD` only |
| Dates allowed | Every date present under `data/live_week/` (2026-08-09 → 2026-08-14) |
| Loader mode | `mechanical` — the live week is 2026 data and therefore inside the seal |
| Scored | **No.** Mechanical mode can never emit a scorecard (ruling A) |
| Timeframe | Ticks |
| Seed | 20260819 |
| Re-run class | `full` |
| Entry point | `research.spread_session:run` |
| Experiment dir | `experiments/T0-spread-by-session/` |
| Deliverable | `reports/T0_spread_by_session.md` |

## Non-goals — explicitly out of scope for this card

* Any statement about where edge might live, at any horizon, for any pair.
* Any comparison across pairs, or any use of these numbers to rank anything.
* Ingesting any data at all. T0 reads what Phase 1 already stored; if the live
  week is missing, the card fails rather than fetching it.
* Touching `data/research/`, which is empty until T2.
* Proposing follow-up work. If something looks worth chasing, it goes in the
  report as an observation for review, not as a next experiment.

## Definition of done

```
env_fxlab\Scripts\python.exe -E -s -m research.run --config experiments/T0-spread-by-session/config.toml
env_fxlab\Scripts\python.exe -E -s verify2\research_gate.py experiments/T0-spread-by-session
```

Gate exit 0, report written, ledger carrying one start and one end record for
this card.

## Review questions for the checkpoint

1. Do the session medians reproduce the Phase 1 coverage report exactly? If
   not, one of the two paths is wrong and both are suspect.
2. Does anything about the protocol itself need changing before T1 — the
   config shape, the ledger fields, the report layout?
