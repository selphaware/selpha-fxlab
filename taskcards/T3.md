# T3 — Data quality, holiday calendar, cross-checks (full store, 2005–2025-02)

Bounded loop task. Scope is this card; the agent may not extend it.

## Rulings pre-registered at the M2 checkpoint (2026-09-05) — write into SPEC2.md §Harness rulings

R1. **AUDUSD before 2011-01-01 is EXCLUDED from research.** The crossed-quote
    corruption (two episodes, 2007-04→2008-09 and 2009-04→2010-10) is NOT
    recovered; the hour-level CROSSED_QUOTE rule is unchanged. The research
    loader refuses AUDUSD requests dated before 2011-01-01 with reason
    `PAIR_EXCLUDED_WINDOW`; the exclusion is recorded in the manifests and
    stated on every report and scorecard that touches AUDUSD. Cross-pair
    analyses spanning pre-2011 run on eleven pairs there.
R2. **JPY pre-open hours stay rejected.** The 16 USDJPY/EURJPY Sunday-21:00Z
    winter hours keep CLOSED_MARKET_TICK; they gain the sub-label
    `PRE_OPEN_FEED_DATA` in the gap table. The boundary rule is untouched.
R3. **Spread-regime comparisons across eras must control for ticks per hour**
    (medians / p90 / fixed-sample statistics, never raw SPREAD_OUTLIER
    counts). A T5 requirement, stated here so the T3 report frames its
    spread tables accordingly.
R4. **Tick counts are not a volume or activity proxy** until a T4 card has
    characterised the density series. No report or scorecard may use them
    as such before then.
R5. **The holiday calendar derives from manifest `status == "empty"`,** never
    from the EMPTY_TRADING_HOUR warning list (the audit found warnings 62%
    short of empties in T2b).
R6. **No hand-written numbers in reports.** Every figure is derived at render
    time from manifests / ledger / result.json.

## Step 0 — Report reconciliation (before any new analysis)

Fix the six audit defects, then regenerate and re-gate BOTH ingestion
experiments (their result hashes will change; the committed hashes must be
updated with them):
1. `research/ingest_report.py` hardcodes "T2a" — parameterise by task card;
   T2b's report must name T2b everywhere (title, card link, trial count,
   provenance paths).
2. `bar_rows_built` scoped to the experiment's window, not the whole store.
3. Prose bullets replaced by derived figures (R6): the AUDUSD split
   (12,996 crossed AUDUSD / 2 USDJPY / 1 FETCH_ERROR inside episode 1),
   episode totals 6,227 + 6,769, and the by-year SPREAD_OUTLIER table
   rendered from one source.
4. Warning ledgers: root-cause the disagreement between shard-level
   `validation.warnings` and per-hour `issues[]` (27 T2b warnings-only, 67
   issues-only; EMPTY_TRADING_HOUR and TICK_COUNT_OUTLIER absent from
   issues[] entirely). Declare ONE canonical source in SPEC2, make the
   reports read only that, and document what the other is for. If the fix
   needs a manifest-format change, that is a checkpoint decision — propose,
   do not change.
5. Throughput tables: requests and seconds from the same source
   (sessions.jsonl), or clearly labelled if not.
6. Cross-check the fix by re-running the audit's reconciliation (manifests
   vs reports vs disk) and including its table in the T3 report.

## Step 1 — Full-store validation pass

Re-validate every stored hour offline against the Phase 1 rules (ask ≥
bid > 0, UTC monotonic, no closed-market rows, pinned schema, row counts
vs manifest). Expected: zero new failures. Any failure is a hard stop —
report, do not repair.

## Step 2 — Holiday calendar (pre-reg #5)

Build `config/calendar.toml` (tracked; versioned) from R5: hours the derived
week calls open that were served empty for ALL twelve pairs (eleven where
AUDUSD is excluded) on the same date → candidate holiday; intersect with a
static list of major FX holidays (New Year, Good Friday, Easter Monday,
Christmas, Boxing Day, US Thanksgiving/July 4 partials, etc.) and report
the two sets' agreement and disagreements. Deliver: the calendar, the
derivation table, and — separately — the empty hours NOT explained by the
calendar (single-pair empties, partial days), which are data facts for T4,
not holidays. After this card, EMPTY_TRADING_HOUR on a calendar date is
`closed`, not a warning.

## Step 3 — OANDA cross-check (pre-reg #7)

Using `OANDA_API_TOKEN` (fxpractice ONLY; host from OANDA_ENV; read-only
instruments/candles endpoints; token never logged): for each pair, sample
≥ 24 hours per year spread across sessions, compare Dukascopy hourly mid
(from stored ticks) with OANDA H1 mid. Threshold 1.0 pip; the derived
16:00–18:00 America/New_York roll window exempt. Report: the difference
distribution per pair per year, every hour beyond threshold, and OANDA's
own history availability per pair (some instruments may not reach 2005 —
report coverage honestly rather than assuming). Any non-roll hour beyond
threshold BLOCKS that data from research use pending review (pre-reg #7).

## Deliverable

`reports/T3_data_quality.md` (all numbers derived): reconciliation table,
validation results, calendar derivation and exceptions, cross-check
distributions, AUDUSD exclusion stated, R1–R6 recorded. Plus the
regenerated T2a/T2b reports, `config/calendar.toml`, and the ledgered
experiment under `experiments/T3-quality/` (scoring-mode config,
scored=false).

## Non-goals

No EDA, no statistics beyond validation and calendar counts, no strategy
content, no widening of any ceiling or rule (R1 keeps CROSSED_QUOTE as is),
no AUDUSD re-fetch, no P0-A (USD accounting — its own card before any
scoring), no requests past the seal, no touching verify/ or verify2/
(propose via verify2/proposed/ if a gate change is needed).

## Done

Both ingestion experiments re-gated exit 0 with updated hashes; research
gate exit 0 on experiments/T3-quality; calendar committed; report written;
ledger committed and pushed. Stop. Do not begin T4.