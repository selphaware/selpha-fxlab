# T0 - protocol shakedown. See taskcards/T0.md.
#
# Mechanical mode: the Phase 1 live week is 2026 data, which is inside the
# holdout seal. Pre-registered decision #2 allows it for pipeline and
# mechanical checks and forbids it for strategy scoring or selection, so this
# experiment is barred from emitting a scorecard by the runner, not by
# convention. No dates are named here; the loader reads what is present.

[experiment]
id = "T0-spread-by-session"
taskcard = "T0"
entry = "research.spread_session:run"
seed = 20260819
mode = "mechanical"
scored = false
rerun_class = "full"
data_root = "data/live_week"

[experiment.params]
pair = "EURUSD"
